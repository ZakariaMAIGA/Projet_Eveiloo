import 'package:flutter/material.dart';

class TuturialParentPage extends StatelessWidget {
  const TuturialParentPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}